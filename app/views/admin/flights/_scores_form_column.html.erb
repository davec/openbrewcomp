<%
  # HACK: This file exists to avoid displaying the scores in the update form
  # since they are included as part of the entries instead.
  #
  # Without the inclusion of :scores in active_scaffold_config.update.column,
  # which necessitates this dummy file, the scores won't get saved.
%>
